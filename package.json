{
  "name": "gachiroun-or-kr",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:wrangler": "wrangler pages dev --compatibility-date=2024-01-01 -- astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev dist",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=gachiroun-or-kr",
    "deploy:preview": "npm run build && wrangler pages dev dist"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.10",
    "@astrojs/react": "^4.4.2",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "astro": "^5.15.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
